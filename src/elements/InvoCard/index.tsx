import React from 'react';
import styles from './styles.module.css';

const InvoCard: React.FC = () => {
  return (
    <div className={styles.title}>
      Hello, CSS Modules in Vite with React and TypeScript!
    </div>
  );
};

export default InvoCard;
